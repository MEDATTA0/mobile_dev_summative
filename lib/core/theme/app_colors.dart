import 'package:flutter/material.dart';

/// Brand palette sampled from the EmpowerHER Figma design.
class AppColors {
  static const primary = Color(0xFF177A56); // brand green
  static const primaryDark = Color(0xFF0F5C40); // deep green (hero cards)
  static const primaryLight = Color(0xFFE3F2EB); // pale green (chips, fills)
  static const secondary = Color(0xFFDC5B32); // terracotta accent
  static const background = Color(0xFFF7F7F4); // app background
  static const surface = Color(0xFFFFFFFF); // cards, header
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B7280);
}
